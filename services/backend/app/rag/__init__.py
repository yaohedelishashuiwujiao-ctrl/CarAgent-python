"""RAG pipeline package.

The runnable platform implementation lives in ``backend.app.services.rag``.
This package stays intentionally import-safe while optional external vector-store
adapters are introduced later.
"""
